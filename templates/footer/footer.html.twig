<ul style="list-style-type:none; display: flex; justify-content: space-around; padding: 0; margin: 0; background-color: #f1f1f1; border-top: 1px solid #ccc; border-bottom: 1px solid #ccc;">
    {% for item in footers %}
        <li><a href="{{path(item.enlace)}}" shape="rect">{{item.nombre}}</li>
    {% endfor %}
</ul>